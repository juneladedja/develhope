import { InteractiveWelcome } from './component/InteractiveWelcome'
import './App.css'

function App() { 
  // creo funzione App che mi deve ritornare la funzione che ho creato interactiveWelcome in cui ci sono tutti i ragionamenti

  return (
    <InteractiveWelcome/> 
  )
}

// nel file App.jsx, quando esporto, scrivo sempre default
export default App