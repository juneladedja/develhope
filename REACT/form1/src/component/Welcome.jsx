export function Welcome({ name }) {
    return(
        <div>
            <h2>Welcome {name}</h2>
        </div>
    )
}

// la funzione la scrivo con export
// cartella del componente welcome quindi scrivo solo questo
// creo funzione welcome a cui metto prop name
// poi nell'h2 dovra comparire welcome + nome che viene dato